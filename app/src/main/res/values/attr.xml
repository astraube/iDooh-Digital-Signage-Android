<?xml version="1.0" encoding="utf-8"?>

<resources>

    <!-- https://github.com/developer-shivam/FeaturedRecyclerView -->
    <declare-styleable name="RoundedButton">
        <attr name="buttonColor" format="color" />

        <attr name="buttonGradientStartColor" format="color" />
        <attr name="buttonGradientEndColor" format="color" />

        <attr name="buttonCornerRadius" format="dimension" />

        <attr name="buttonRippleColor" format="color" />
        <attr name="buttonRippleAlpha" format="integer" />
    </declare-styleable>

    <declare-styleable name="CenteringRecyclerView">
        <attr name="ignoreIfVisible" format="boolean" />
        <attr name="ignoreIfCompletelyVisible" format="boolean" />
    </declare-styleable>

    <!-- https://github.com/pedant/sweet-alert-dialog -->
    <declare-styleable name="Rotate3dAnimation">
        <attr name="rollType" format="enum">
            <enum name="x" value="0"/>
            <enum name="y" value="1"/>
            <enum name="z" value="2"/>
        </attr>
        <attr name="fromDeg" format="float" />
        <attr name="toDeg" format="float" />
        <attr name="pivotX" format="fraction"/>
        <attr name="pivotY" format="fraction" />
    </declare-styleable>

    <!-- https://github.com/Todd-Davies/ProgressWheel -->
    <declare-styleable name="ProgressWheel">
        <attr name="pwText" format="string" />
        <attr name="pwTextColor" format="color" />
        <attr name="pwTextSize" format="dimension" />
        <attr name="pwBarColor" format="color" />
        <attr name="pwRimColor" format="color" />
        <attr name="pwRimWidth" format="dimension" />
        <attr name="pwSpinSpeed" format="integer" />
        <attr name="pwCircleColor" format="color" />
        <attr name="pwRadius" format="dimension" />
        <attr name="pwBarWidth" format="dimension" />
        <attr name="pwBarLength" format="dimension" />
        <attr name="pwDelayMillis" format="dimension"/>
        <attr name="pwContourColor" format="color"/>
        <attr name="pwContourSize" format="float"/>
    </declare-styleable>

    <!-- Component floatlabelededittext -->
    <declare-styleable name="FloatLabeledEditText">
        <attr name="fletTextAppearance" format="reference" />
        <attr name="fletPadding" format="reference|dimension" />
        <attr name="fletPaddingLeft" format="reference|dimension" />
        <attr name="fletPaddingTop" format="reference|dimension" />
        <attr name="fletPaddingRight" format="reference|dimension" />
        <attr name="fletPaddingBottom" format="reference|dimension" />
        <attr name="fletBackground" format="reference|color" />
    </declare-styleable>

    <!-- Component FabTransitionActivity -->
    <declare-styleable name="FooterLayout">
        <attr name="ft_color" format="color"/>
        <attr name="ft_anim_duration" format="integer"/>
        <attr name="ft_container_gravity" format="enum">
            <enum name="start" value="0"/>
            <enum name="center" value="1"/>
            <enum name="end" value="2"/>
        </attr>
        <attr name="ft_fab_type" format="enum">
            <enum name="mini" value="40"/>
            <enum name="normal" value="56"/>
        </attr>
    </declare-styleable>

    <!-- Componente SeekBrPopup -->
    <declare-styleable name="SeekBarPopup">
        <attr name="android:orientation"/><!-- nao esta sendo utilizado -->
        <attr name="sbp_autoHideEnabled" format="boolean" />
        <attr name="sbp_autoHideTime" format="integer" />

        <attr name="sbp_topImage" format="reference"/>

        <attr name="sbp_triggerButtonSize" format="dimension"/>
        <attr name="sbp_triggerButtonImage" format="reference"/>
        <attr name="sbp_triggerButtonStyle" format="reference"/>
        <attr name="sbp_triggerButtonBackground" format="reference"/>

        <attr name="sbp_seekBarSize" format="dimension"/>
        <attr name="sbp_seekBarBackground" format="reference"/>
        <attr name="sbp_seekBarShowAnimation" format="reference" />
        <attr name="sbp_seekBarHideAnimation" format="reference" />
    </declare-styleable>

    <!-- Componente CircleIndicator -->
    <declare-styleable name="CircleIndicator">
        <attr name="ci_width" format="dimension"/>
        <attr name="ci_height" format="dimension"/>
        <attr name="ci_margin" format="dimension"/>
        <attr name="ci_animator" format="reference"/>
        <attr name="ci_animator_reverse" format="reference"/>
        <attr name="ci_drawable" format="reference"/>
        <attr name="ci_drawable_unselected" format="reference"/>

        <attr name="ci_orientation" format="enum">
            <!-- Defines an horizontal widget. -->
            <enum name="horizontal" value="0"/>
            <!-- Defines a vertical widget. -->
            <enum name="vertical" value="1"/>
        </attr>

        <attr name="ci_gravity">
            <!-- Push object to the top of its container, not changing its size. -->
            <flag name="top" value="0x30"/>
            <!-- Push object to the bottom of its container, not changing its size. -->
            <flag name="bottom" value="0x50"/>
            <!-- Push object to the left of its container, not changing its size. -->
            <flag name="left" value="0x03"/>
            <!-- Push object to the right of its container, not changing its size. -->
            <flag name="right" value="0x05"/>
            <!-- Place object in the vertical center of its container, not changing its size. -->
            <flag name="center_vertical" value="0x10"/>
            <!-- Grow the vertical size of the object if needed so it completely fills its container. -->
            <flag name="fill_vertical" value="0x70"/>
            <!-- Place object in the horizontal center of its container, not changing its size. -->
            <flag name="center_horizontal" value="0x01"/>
            <!-- Grow the horizontal size of the object if needed so it completely fills its container. -->
            <flag name="fill_horizontal" value="0x07"/>
            <!-- Place the object in the center of its container in both the vertical and horizontal axis, not changing its size. -->
            <flag name="center" value="0x11"/>
            <!-- Grow the horizontal and vertical size of the object if needed so it completely fills its container. -->
            <flag name="fill" value="0x77"/>
            <!-- Additional option that can be set to have the top and/or bottom edges of
            the child clipped to its container's bounds.
            The clip will be based on the vertical gravity: a top gravity will clip the bottom
            edge, a bottom gravity will clip the top edge, and neither will clip both edges. -->
            <flag name="clip_vertical" value="0x80"/>
            <!-- Additional option that can be set to have the left and/or right edges of
            the child clipped to its container's bounds.
            The clip will be based on the horizontal gravity: a left gravity will clip the right
            edge, a right gravity will clip the left edge, and neither will clip both edges. -->
            <flag name="clip_horizontal" value="0x08"/>
            <!-- Push object to the beginning of its container, not changing its size. -->
            <flag name="start" value="0x00800003"/>
            <!-- Push object to the end of its container, not changing its size. -->
            <flag name="end" value="0x00800005"/>
        </attr>

    </declare-styleable>

</resources>
